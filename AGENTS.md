# Repository operating rules

- 本仓库的交付顺序固定为：本地网页 → 阿里云网页 → 微信小程序 → 原生 App 决策。
- 产品正确性、可追溯性、隐私和用户确认优先于 Agent 自主性与展示效果。
- 大模型不得生成自由 SQL、直接调用第三方下单接口、修改行程事实或生成未经证据支持的时间。
- 出发时刻、机场流程预算、交通分位数和风险缓冲必须由确定性代码计算并由 Verifier 核验。
- 所有外部事实必须带来源、采集时间、适用范围、完整性和新鲜度；缺失时不得用模型记忆补齐。
- 客户端不得保存模型密钥、地图 Key、数据库密码、AppSecret、第三方登录票据或共享 Bearer Token。
- 第三方 App 数据只可来自用户主动导入、系统明确授权、官方 API 或书面批准的数据合作。
- 不得提交真实个人行程、精确住址/轨迹、订单号、票号、身份证件、手机号、生产截图或原始审计。
- 合成数据和脱敏 Gold Cases 是默认基线；真实数据、通知、预约、付费、公开发布和生产晋级需单独门禁。
- Git 分支使用 `codex/` 前缀；功能通过 Pull Request 合并，禁止直接向 `main` 推送业务修改。
- 每次实现必须同时更新契约、测试、威胁模型、文档和版本化能力注册表。
- 完成代码阶段前至少运行：后端单测、Ruff、契约校验、前端 lint/typecheck/build、Playwright 和秘密扫描。
