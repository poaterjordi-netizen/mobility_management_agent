# 大模型 Agent 出行管家：完整实施计划

版本：0.1
基线日期：2026-07-27
目标目录：`/Users/xiaobosun/software/mobility-management-agent`

当前实现状态：基础框架已进入 `P1/P3/P4` 工程验证。网页、API、合成确定性引擎、Verifier、
微信小程序与阿里云容器化入口已经落地；真实数据、提醒、账号和外部动作仍按本文门禁推进。

## 1. 目标与产品边界

产品首先解决一个高价值问题：

> 已知用户的航班、机场、出发地和风险偏好，系统在航班前一天给出可解释的出发时刻，
> 持续吸收航班、机场流程、道路、天气和城市事件变化，并询问用户是否需要预约车辆。

首版不是“控制所有手机 App 的万能 Agent”，而是一个可靠的机场接驳决策系统。它必须：

- 发现或接收行程；
- 核对航班与航站楼；
- 获取交通、天气、机场流程和事件证据；
- 计算带置信度和安全余量的出发时刻；
- 解释“为什么是这个时间”；
- 在用户明确同意后打开官方预约入口；
- 在证据不足、数据冲突或风险过高时主动降级或请求确认。

首版明确不做：

- 抓取携程、航旅纵横、微博、小红书等 App 的私有页面；
- 保存第三方账号密码、Cookie 或登录态；
- 让大模型自由操作手机、自由浏览或自由生成 SQL；
- 无人确认自动叫车、付款、退改签或发送社交消息；
- 宣称能实时获得所有登机口、安检排队或封路信息；
- 在未完成真实回测前承诺“不会误机”。

## 2. 成功定义

产品成功不是“能对话”，而是下面这条纵向链路稳定成立：

```text
行程导入
  → 用户确认关键字段
  → 多源事实获取与冲突处理
  → 确定性出发时刻计算
  → EvidencePacket
  → Verifier
  → 可解释建议
  → T-24h 提醒
  → 用户确认后跳转预约
  → 全链路可回放
```

第一阶段北极星指标：

- 有效行程中，出发建议生成成功率；
- 建议按时送达率；
- 用户采纳或主动调整率；
- 建议到达机场时间的校准度；
- 因数据缺失而产生的安全降级率；
- 每个关键时间结论的证据覆盖率；
- 用户对“为什么此时出发”的理解率。

## 3. 继承客流智能体的部分

以参考仓库提交 `13aaa26` 为工程基线，继承：

- 单仓库、`src/` 布局、FastAPI、React/TypeScript/Vite、OpenAPI 生成客户端；
- `SemanticFrame → OperationIR → TaskPlan → ToolResult → EvidencePacket → Verifier`；
- ToolRegistry 白名单、FakeProvider、模型 Provider 抽象、Trace Store；
- 合成数据先行、真实数据 shadow、production promotion gate；
- Python/前端/容器/文档 GitHub Actions；
- Nginx 固定 HTTPS 入口、同源 API、限流、健康检查和故障可解释；
- Git 中不保存秘密、生产数据、原始审计或私有基础设施材料。

需要改变：

- 客流查询域替换为 Trip、Flight、Airport、Route、Disruption、Decision 和 Reminder 域；
- 核心事实来源由单一数据库变为多源时效数据；
- 引入用户写数据，但与模型工具和外部事实库严格分离；
- 核心决策从 QueryIR 聚合改为可回测的时序约束/分位数引擎；
- 增加提醒调度、幂等 Outbox、用户同意和操作提案；
- 对行踪轨迹、住址和行程执行更高等级的敏感个人信息保护。

不复制参考工程的临时做法：

- 不把个人 Mac + Hermes + 反向隧道当作 7×24 小时生产方案；
- 不复用数据库账号、数据库名、端口、Basic Auth 或小程序 AppID；
- 不在同一 Nginx 限流桶中混用普通 API 与长时间模型请求；
- 不以浏览器能打开域名代替小程序真机验收；
- 不以合成数据或规则系数冒充真实预测能力。

## 4. 技术选型

### 4.1 应用栈

| 层 | 选型 | 原因 |
| --- | --- | --- |
| Web | React 19、TypeScript、Vite、TanStack Query、ECharts | 与参考项目一致，便于复用工程经验 |
| API | Python 3.11+、FastAPI、Pydantic | 契约清晰、易生成 OpenAPI |
| 领域/Agent | 纯 Python 包 + 显式状态机 | 可测试、可回放、避免初期微服务复杂度 |
| 数据库 | MySQL，独立库/独立账号；本地可用 SQLite fixture | 与现有资源兼容，真实个人数据不与 metroflow 混库 |
| 文件 | 本地受控目录；云端使用独立 OSS bucket | 截图/OCR 中间件需生命周期删除 |
| 调度 | 单独 worker + DB Outbox/lease | 首版不引入 Redis/Celery，保证幂等 |
| 入口 | Nginx + HTTPS + 同源 API | 复用客流智能体部署经验 |
| 可观测 | 结构化日志、Trace ID、SLS/CMS | 支持证据审计与故障定位 |

### 4.2 模型

首选质量基线：

- 模型：`gpt-5.6-sol`；
- 接口：Responses API；
- 推理强度：`medium` 起步，并在 Gold Cases 上与 `low` 比较；
- 用途：行程文本/截图结果的语义归一、冲突解释、受控任务规划、证据化回答；
- 不启用：Pro、模型内多 Agent、Programmatic Tool Calling、自动持久化推理；
- 默认：`FakeProvider`，真实模型只在显式 shadow/approved 环境启用。

模型不得计算最终出发时刻，不得替代实时数据源，不得收到完整住址、票号、证件号或原始
通知。云端若调用境外模型，必须先通过数据最小化和数据出域门禁。若该门禁未通过，
核心功能继续使用确定性解析/规则，另以阿里云百炼的境内模型做独立对照评测；替换只发生
在 Provider 层，且必须通过同一批 Gold Cases。

### 4.3 单 Agent 而非自由多 Agent

第一版使用一个模型、多个逻辑角色和一个确定性状态机：

```text
UNDERSTAND → LINK → PLAN → FETCH → ASSESS → COMPUTE
           → SYNTHESIZE → VERIFY → PROPOSE → RESPOND
```

外部数据可并行拉取，但不让多个自治 Agent 相互转交高风险决定。以后只有当评测证明独立
并行调查能显著提升质量且不损害证据链时，才考虑多 Agent。

## 5. 产品表面交付顺序

### P0：工程与契约基线（2 周）

时间：2026-07-27 至 2026-08-09。

交付：

- 仓库、CI、文档站、模块骨架、合成数据；
- 核心 Schema、能力注册表、威胁模型；
- FakeProvider、FakeFlight、FakeAirport、FakeRoute、FakeWeather；
- 20 条确定性出发时刻 Gold Cases；
- 本地环境一条命令启动的空壳页面与健康检查（进入实施阶段后完成）。

退出条件：

- 契约可机器校验；
- 无真实个人数据；
- 同一输入可重放并得到相同决策；
- 关键风险和数据门禁都有负责人。

### P1：本地网页纵向 MVP（4 周）

时间：2026-08-10 至 2026-09-06。

范围：

- 手工录入航班/出发地；
- 上传行程截图并确认提取结果；
- 合成航班、机场、道路、天气、事件；
- 确定性出发时刻计算；
- 建议卡、证据列表、时间线、风险/置信度；
- T-24h 提醒预览，不真实发送；
- “打开高德/滴滴”深链提案，不自动下单。

退出条件：

- 50 条端到端 Gold Cases 通过；
- 所有时间有时区、来源、采集时刻；
- 模型关闭时仍能完成核心闭环；
- Playwright 覆盖创建行程、确认、计算、冲突、失败和删除。

### P2：本地网页真实数据 Shadow（4 周）

时间：2026-09-07 至 2026-10-04。

范围：

- 接高德路径/天气的正式测试 Key；
- 至少一个获授权的航班/机场信息来源；
- 首批机场数字孪生（建议 3 个）；
- 公告/新闻/活动信号适配器；
- 定时刷新和变更检测；
- 用实际出行进行人工监督 shadow，不对外承诺。

退出条件：

- 数据许可、配额、TTL、降级和缓存策略记录完整；
- 真实与合成标签完全分离；
- 30 次以上实际或回放行程形成校准报告；
- 没有社交平台未授权抓取。

### P3：阿里云网页（4 周）

时间：2026-10-05 至 2026-11-01。

范围：

- 新子域名（建议 `mobility.9m-zx.com`，实际启用需确认）；
- 在客流项目同一阿里云账号、北京地域和 VPC 内部署独立 Compose 栈；
- 复用既有 Web ECS 仅限低流量 staging，并先做容量门禁；
- 独立 MySQL 库/账号、OSS bucket、日志项目和运行用户；
- HTTPS、认证、限流、CORS、备份、监控、告警；
- GitHub tag → 构建 → 不可变制品 → 人工批准 → 部署 → 回滚。

退出条件：

- Mac 关机后云端核心页面和 API 仍可用；
- 公网没有数据库端口、内部 API 或管理端口；
- 备份恢复、容器重启、外部 API 超时和模型降级通过演练；
- 真实敏感数据的云端范围已书面批准，否则仅用合成/脱敏数据。

### P4：微信小程序（5 周）

时间：2026-11-02 至 2026-12-06。

范围：

- 与 Web 共用 OpenAPI 和后端；
- 行程录入、截图上传、建议卡、证据、设置、反馈；
- 微信登录交换与对象级授权；
- 订阅消息只在用户授权后发送；
- request 合法域名、隐私协议、备案、体验版真机验收；
- 跳转地图/打车官方入口，不控制其他 App。

退出条件：

- 健康、能力、登录/session 三段握手；
- Android 与 iPhone 各至少一台冷启动真机验证；
- 严格合法域名校验下通过；
- 订阅消息、撤销授权、删除行程、过期截图清理通过。

### P5：小规模试点与 App Go/No-Go（4 周）

时间：2026-12-07 至 2027-01-03。

范围：

- 20–50 名受邀用户；
- 3–5 个主要机场；
- 记录建议、实际出发/到达、采纳、调整和异常；
- 校准交通分位数、机场流程和个体缓冲；
- 完成法务、数据合作、成本、SLO 和事故响应评审。

2027-01-04 做原生 App 决策。只有以下条件同时满足才启动：

- Web/小程序证明用户持续使用；
- 主要数据源有稳定商业或官方授权；
- Notification/Calendar/Share Extension 的增益可量化；
- 隐私影响评估与商店政策可通过；
- 团队能承担 Android/iOS 双端持续维护。

## 6. 首批业务能力

能力分三层：

### A. 必须可靠

- 创建、确认、编辑、删除行程；
- 航班/航站楼/计划时间标准化；
- 路线 ETA 与分位数；
- 天气和道路风险；
- 机场流程预算；
- 出发时刻计算；
- T-24h 建议和变化提醒；
- 证据、冲突、数据过期和降级说明。

### B. 有条件启用

- 截图 OCR；
- 航班动态、登机口、安检/值机信息；
- 大型活动、道路施工、封路和事故信号；
- 机场室内步行距离；
- 用户历史个性化；
- 地图/网约车深链。

### C. 以后再做

- 邮箱/日历 OAuth 自动发现；
- Android 通知监听；
- iOS Share Extension/Shortcuts；
- 官方网约车下单 API；
- 退改签和行中自动处置；
- 多航段、国际旅行、火车和酒店；
- 原生 App。

## 7. 数据优先级

按可信度与可持续性排序：

1. 用户确认过的行程字段；
2. 航空公司、机场、地图、气象、政府公告等官方/合同 API；
3. 用户主动提供的截图、通知、日历或邮件；
4. 获平台许可的公共内容 API；
5. 新闻和公开网页检索，仅作风险线索，不能单独形成硬性时间结论；
6. 模型常识，只能解释，不得作为实时事实。

社交媒体只作为弱信号。未获得平台 API 或书面许可前，不做账号登录、批量抓取、反爬
绕过或页面自动化。

## 8. 决策原则

出发建议由硬约束和概率预算组成：

```text
建议出发时间
= 要求到达航站楼时间
− 交通时间分位数
− 叫车等待分位数
− 个体/风险缓冲
```

要求到达航站楼时间同时满足值机截止、安检、步行、登机截止等约束。模型只把结果解释成
自然语言。详细公式见 [决策引擎](docs/decision_engine.md)。

## 9. GitHub 管理

已确定使用公开仓库：

```text
poaterjordi-netizen/mobility_management_agent
```

本地仓库使用 `main`，开发使用 `codex/<issue>-<slug>`，所有合并走 Pull Request。
公开可见不等于可提交真实行程、秘密或正式微信 AppID。

Milestone：

- `P0-foundation`
- `P1-local-web-mvp`
- `P2-real-data-shadow`
- `P3-aliyun-web`
- `P4-wechat-miniprogram`
- `P5-pilot`
- `P6-native-app-decision`

每个 PR 必须关联 Issue、包含验收证据、无秘密，并通过 CI。云端部署只允许来自已签名
或受保护 tag，并保留源码 commit、镜像 digest、配置版本和数据库迁移版本。

## 10. 人员与周期假设

上述 23 周日历基于：

- 1 名后端/Agent 工程师；
- 1 名前端/全栈工程师；
- 1 名产品/设计/测试角色（可兼职）；
- 数据合作、法务和云运维按里程碑参与。

若只有 1 名工程师，预计日历时间为 40–50 周；不要靠削减测试、隐私或数据授权来压缩。

## 11. 成本控制

不在计划阶段写死价格，实际费用以阿里云/OpenAI/地图/航班数据控制台和合同为准。
每月成本表至少分开记录：

- ECS、云盘、流量、域名/证书；
- MySQL/RDS、OSS、备份、SLS/CMS；
- 地图/天气/航班/机场数据 API；
- 模型输入、输出、缓存和失败重试；
- 微信认证、短信/邮件/推送；
- 人工数据维护和客服。

门禁：

- staging 首先复用既有资源，但必须设 CPU/内存/磁盘/并发阈值；
- 模型和外部 API 均设日/月预算、并发、超时和熔断；
- 在试点前形成“单次有效建议成本”和“单活跃用户月成本”；
- 任何新付费服务、扩容或长期合同均需负责人确认。

## 12. 后续阶段需要负责人确认的三项

这些不阻塞本计划成立，但会阻塞 P2/P3：

1. 首批机场：建议北京首都、北京大兴、上海浦东，或替换为真实高频机场；
2. `gpt-5.6-sol` 云端调用是否允许发送最小化的境内个人行程元数据；
3. 航班/机场动态数据采用现有商业账号、合作接口，还是先只用用户确认与公开信息。

## 13. 完成定义

项目只有在以下条件满足时才可称为“试点可用”：

- 同一真实行程在 Web 和小程序得到一致、可解释、可回放的决定；
- 关键事实全部有来源、时间和完整性；
- 不完整/冲突/过期数据会保守降级；
- 用户可以撤回授权、删除数据和关闭提醒；
- 自动动作停在用户确认之前；
- 全量 Gold Cases、前后端测试、恢复演练和隐私检查通过；
- 云端不依赖个人 Mac 在线；
- 当前能力和未完成边界在产品页面中明确展示。
